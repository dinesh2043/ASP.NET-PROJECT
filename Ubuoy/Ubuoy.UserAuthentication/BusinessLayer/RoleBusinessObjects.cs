using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using Ubuoy.UserAuthentication.DataLayer;

namespace Ubuoy.UserAuthentication.BusinessLayer
{
    public class RoleBusinessObjects
    {
        /**
        public List<Role> GetRole()
        {
            return new RoleRepository().GetRole();
        }
        public Role GetRoleById(Guid id)
        {
            return new RoleRepository().GetRoleById(id);
        }

        public Role InsertRole(string name, string description)
        {
            return new RoleRepository().InsertRole(name, description);
        }
        public Role DeleteRole(Guid id)
        {
            return new RoleRepository().DeleteRole(id);
        }
        public Role UpdateRole(Guid id)
        {
            return new RoleRepository().UpdateRole(id);
        }
         * */
    }
}